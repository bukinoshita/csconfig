# csconfig

## How to use

### Save the files in:
```
$ csgo/cfg
```

### Usage

`autoexec.cfg` will automatically run when game initialize.

Run dm server, open the `console` and type:
```
$ exec dm.cfg
```
